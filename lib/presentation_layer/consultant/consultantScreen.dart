import 'package:flutter/material.dart';

class ConsultantScreen extends StatelessWidget {
  const ConsultantScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Consultant screen'),
      ),
    );
  }
}
